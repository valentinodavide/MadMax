<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polito.mad17.madmax">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.GroupsActivity">
        </activity>

        <activity android:name=".activities.GroupExpensesActivity">
        </activity>

        <activity android:name=".activities.NewExpenseActivity">
        </activity>

        <activity android:name=".activities.FriendDetailActivity">
        </activity>

        <activity android:name=".activities.FriendsActivity">
        </activity>

        <activity android:name=".activities.NewGroupActivity">
        </activity>
        <activity android:name=".activities.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.EmailVericationActivity"></activity>

    </application>


</manifest>